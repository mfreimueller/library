server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://borrow-db:3306/borrows
    username: borrowuser
    password: borrowpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka